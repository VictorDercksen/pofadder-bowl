# Pofadder Bowl 2026 · Handoff (2026-09-17)

Private fantasy-league punishment app for “Show Us Your TD’s”. Victor Dercksen travels Malmesbury → Pofadder → Malmesbury on 23–25 September 2026, runs 14 km and completes ten proof challenges worth 100 points while the league watches, reacts, plays Punishment Bingo and predicts results.

## State at handoff

| Item | Value |
|---|---|
| Production | https://pofadder-bowl.vercel.app (Vercel project `pofadder-bowl`, scope `victor-4043s-projects`; `main` auto-deploys, `vercel deploy --prod --yes` also works) |
| Public routes | `/teaser` (poster + live kickoff countdown), `/demo/*` (labelled in-memory demo), `/login`, `/recap/public/<league>/<event>` (only when published and consented) |
| Repo | https://github.com/VictorDercksen/pofadder-bowl (`main`, all work committed) |
| Supabase | project ref `nsiqnqlkaelskimjeyed`, org “Pofadder Bowl”, region eu-central-1, free tier; all six migrations and `seed.sql` applied |
| Local stack | `npx supabase start` (Docker), `npm run db:reset`, `npm run seed:local-fixtures`; fixture password `pofadder-local-2026` |
| Secrets | Hosted keys and DB password are in the ignored files `.env.hosted` and `.env.local` in the repo folder on Victor’s machine; never commit them |
| Admin | victordercksen@gmail.com is the only admin (also commissioner and event participant) |

## Roles (enforced in RLS and RPCs, never client-side)

- **admin** · invites, roles, event participant, Sleeper import and link confirmation, event settings, prediction rules. Only Victor. Cannot remove own admin.
- **commissioner** · refereeing: review/approve/flag evidence, penalties, official results, resolve predictions, confirm bingo incidents, issue certificate. Admins inherit this.
- **participant** · Victor for this event: uploads, check-ins, press answers, certificate consent.
- **member** · everyone else: feed comments and reactions, bingo proposals, predictions.

Members claim one NFL franchise at first sign-in (`/choose-team`); a franchise is unique per league (`claim_kit` RPC + unique index). The badge and kit colours follow the member across posts, review, bingo, predictions, press room and the mobile drawer.

## Authentication

- Sleeper has no OAuth; it is read-only. Auth is Supabase invite-only magic links. Sleeper managers are imported (league `1313900125680054272`) and linked to accounts by the admin.
- `/auth/confirm` accepts `token_hash` (custom templates), PKCE `?code=`, and implicit `#access_token` links (via `/auth/session`). Free-tier Supabase blocks custom email templates with the default mailer, so production uses default templates and the implicit path.
- **Blocker for inviting the league:** Supabase’s default mailer only delivers to the Supabase organisation’s own members. Configure custom SMTP (Resend/Postmark) under Project Settings → Auth → SMTP before inviting anyone else, or generate invite links with `admin.auth.admin.generateLink` and hand them over.
- Config gotcha: `[auth.email] enable_signup=false` disables the whole email provider (all logins). Invite-only is the top-level `[auth] enable_signup=false` only.

## Pending / next steps

1. **Invite Theo (TheoLotter, the Sleeper league owner) as commissioner, not admin.** Victor is getting his email. Use the Members page or `inviteMember`; with the default mailer the email will not arrive, so generate the link and give it to Victor to forward.
2. Set up custom SMTP, then invite the remaining ten managers and confirm Sleeper links.
3. Optional: swap `NEXT_PUBLIC_MAP_TILE_URL` for a keyed tile provider (currently public OSM tiles).
4. Not exercised end to end in a browser: resumable TUS uploads over 6 MB, IndexedDB drafts, GPS prompting, MediaRecorder press answers, certificate PNG export, Web Share, Realtime in a second browser. All are implemented and render.

## Where things live

| Area | Path |
|---|---|
| Ten private screens | `src/app/(league)/*` (game-centre, my-trip, map, proof, review, bingo, predictions, press, recap, account) plus `review/members`, `choose-team` |
| Server actions | `src/lib/actions/*.ts` (every action re-checks role from `getLeagueContext`) |
| Context and role resolution | `src/lib/league.ts` (`getLeagueContext`, `requireAdmin`, `requireCommissioner`, `requireParticipant`) |
| Design system (ported mockup CSS, jersey v2, buttons, drawer, kit panels) | `src/app/globals.css` |
| Jersey post, member badge, kit panel, marks | `src/components/ui/JerseyCard.tsx`, `Marks.tsx`, `KitPanel.tsx` |
| Shell, sidebar, mobile drawer | `src/components/shell/*` |
| Supabase schema, functions, RLS, storage, kits, admin | `supabase/migrations/2026091600{0100..0500}_*.sql`, `20260917000600_admin_role.sql` |
| Production seed (league, event, itinerary, challenges, bingo, prompts) | `supabase/seed.sql` |
| Supplied programme data and assets | `src/data/league-programme.json`, `public/brand`, `public/nfl`, `public/maps`, `src/fonts` |
| Scripts | `scripts/bootstrap-commissioner.ts` (grants admin), `seed-local-fixtures.ts`, `integration-test.ts`, `auth-flow-test.ts`, `screenshots.ts` |

## Verification commands

```bash
npm run typecheck && npm run lint && npm test      # tsc, eslint (React Compiler rules), vitest (21)
npm run test:integration                             # local stack: RLS, storage, idempotent review, predictions, bingo, kits, admin (22)
npx next build && npx next start -p 3001             # then:
npm run test:auth-flow                               # invite, implicit invite, magic link, sign-out, no leakage (Mailpit)
SCREENSHOT_BASE=http://localhost:3001 npm run screenshots   # 120 captures, overflow + drawer + keyboard checks
```

Run screenshots against `next start`, not `next dev` (dev is too slow and the Realtime socket blocks `networkidle`).

## Cloud sessions (claude.ai/code)

- The Claude GitHub App is installed on the repo (enables `@claude` mentions via `.github/workflows/claude.yml`, PR review via `claude-code-review.yml`, and Auto-fix). The Actions workflows still need an `ANTHROPIC_API_KEY` or `CLAUDE_CODE_OAUTH_TOKEN` repository secret; cloud sessions do not.
- `.mcp.json` (committed) registers Supabase MCP (project `nsiqnqlkaelskimjeyed`, `read_only=true`), Vercel MCP and headless Playwright MCP. Auth headers are injected by Anthropic's proxy from API credentials configured on the cloud environment; no tokens live in the repo.
- Cloud environment (create once at claude.ai/code → cloud icon → add environment): network “Custom” + default list + `*.supabase.co`, `api.supabase.com`, `mcp.supabase.com`, `api.vercel.com`, `vercel.com`, `*.vercel.app`; API credentials: Bearer Vercel token for `api.vercel.com`, Bearer Supabase PAT for `api.supabase.com` and `mcp.supabase.com`; env vars (non-secret): `SUPABASE_PROJECT_REF=nsiqnqlkaelskimjeyed`, `VERCEL_PROJECT_ID=prj_GWtFdazjISRc38qW8e1u29HDJK0t`; setup script: `scripts/cloud-setup.sh`.
- For schema changes from a cloud session use the Supabase CLI (`supabase link --project-ref … && supabase db push`, PAT injected on `api.supabase.com`) rather than the read-only MCP. The database password is not in the repo; migrations via `db push` need it, so prefer opening a PR and letting Victor push migrations locally, or store the password as a cloud API credential.

## Known quirks

- Regenerate types with `npx supabase gen types typescript --db-url postgresql://postgres:postgres@127.0.0.1:54322/postgres` and strip anything before `export type Json`; the `--local` form has hung on this machine.
- `.env.hosted` was once written with CRLF and the stray `\r` broke the league slug on Vercel. Values are clean now; keep env files LF.
- Vercel’s Node version comes from `engines.node` (`22.x || 24.x`) and `.node-version` (24).
- Playwright uses `@playwright/test` with Chromium installed via `npx playwright install chromium`.
